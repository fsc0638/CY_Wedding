.DS_Store
*.log
*.zip
.claude/

# 賓客表單原始 Excel 含個資（電話／地址），不可進公開 repo
# 僅提交去識別化後的 data/wishes.json 與只含雜湊的 data/guests.json
data/*.xlsx
# 含子資料夾（如 data/archive/）內的 xlsx，避免舊名單漏擋
data/**/*.xlsx
# 舊版名單歸檔區，整個資料夾都是個資、一律不進 repo
data/archive/
~$*

# 喜餅兌換券專屬連結清單（含明文賓客姓名），不可進公開 repo
data/voucher_links.txt

# 電子喜帖連結填入用 / 對照表（含明文賓客姓名），不可進公開 repo
data/einvite_links_column.txt
data/einvite_links_reference.csv

# deploy_roster.py 的差異基準快照（含明文賓客姓名），不可進公開 repo
data/.roster_snapshot.json

# Python 轉檔腳本的位元組快取
__pycache__/
*.pyc

# Firebase 服務帳戶金鑰（管理員權限·絕對機密），不可進 repo
data/serviceAccountKey.json
*serviceAccount*.json
*-firebase-adminsdk-*.json

# 每位賓客的喜餅兌換券成品圖（含姓名與兌換碼），供私下發送、不進公開 repo
vouchers/

# 開發過程的暫存測試圖
_test_*.png
_cmp_*.png
_voucher_*.png
